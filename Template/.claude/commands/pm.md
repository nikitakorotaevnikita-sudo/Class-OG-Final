Запусти агента **pm** для оркестрации pipeline разработки прототипа.

Агент pm прочитает `.hypothesis/00_HYPOTHESIS.md`, определит текущее состояние pipeline и спросит что делать дальше (или начнёт автоматически, если передано `MODE: AUTO`).

Дополнительные аргументы: $ARGUMENTS
